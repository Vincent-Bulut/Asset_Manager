<form on:submit|preventDefault={handleSubmit}>
  <input bind:value={form.utUser} placeholder="ut user" />
  <select bind:value={form.role}>
    <option value="" disabled>Select role</option>
    <option value="admin">admin</option>
    <option value="user">user</option>
    <option value="trader">trader</option>
    <option value="pcm">pcm</option>
  </select>
  <input type="email" bind:value={form.email} placeholder="email" />
  <input bind:value={form.fullName} placeholder="full name" />
  <input type="date" bind:value={form.startDate} placeholder="start date" />
  <label>
    <input type="checkbox" bind:checked={form.disabled} /> disabled
  </label>
  <button type="submit">Create User</button>
</form>
