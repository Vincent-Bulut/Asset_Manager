<script>
  import { userDetails } from '$lib/stores';

  let form = {
    utUser: '',
    role: '',
    email: '',
    fullName: '',
    startDate: '',
    disabled: false
  };

  const unsubscribe = userDetails.subscribe(details => {
    if (details) {
      form = {
        utUser: details.ut,
        role: details.role,
        email: details.email,
        fullName: details.full_name,
        startDate: details.start_date,
        disabled: details.disabled
      };
      console.log(form); // Assurez-vous que cela imprime les détails du formulaire mis à jour.
    }
  });

  // ... votre logique de soumission de formulaire ...
</script>
