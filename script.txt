from fastapi import FastAPI, Depends, HTTPException, Security
from fastapi.security.api_key import APIKeyHeader
import os

app = FastAPI()

# Définition de la clé API attendue (idéalement stockée dans une variable d’environnement)
API_KEY = os.getenv("API_KEY", "your-secret-key")
API_KEY_NAME = "X-API-Key"

# Déclaration du header de la clé API
api_key_header = APIKeyHeader(name=API_KEY_NAME, auto_error=True)

# Dépendance pour vérifier la clé API
def verify_api_key(api_key: str = Security(api_key_header)):
    if api_key != API_KEY:
        raise HTTPException(status_code=403, detail="Accès interdit : Clé API invalide")
    return api_key

# 🔥 Appliquer la dépendance globalement à TOUTES les routes 🔥
app = FastAPI(dependencies=[Depends(verify_api_key)])

# Route 1 : automatiquement protégée
@app.get("/secure-data")
def secure_route():
    return {"message": "Vous avez accès aux données sécurisées !"}

# Route 2 : aussi protégée automatiquement
@app.get("/another-endpoint")
def another_route():
    return {"message": "Encore une route protégée !"}
