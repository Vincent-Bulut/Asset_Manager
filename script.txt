
import numpy as np

# Définir les conditions
conditions = [
    self._pivot_steve_cont_mapped['DynamicExportData[Contractual floor + Calculated - CodeNBSP]'].isin(['FLIND', 'NRF', 'NNF']),
    self._pivot_steve_cont_mapped['DynamicExportData[Contractual floor + Calculated - CodeNBSP]'].isin(['FLOOR']),
    self._pivot_steve_cont_mapped['DynamicExportData[Contractual floor + Calculated - CodeNBSP]'].isin(['TUNNEL']),
    self._pivot_steve_cont_mapped['DynamicExportData[Contractual floor + Calculated - CodeNBSP]'].isin(['TOT'])
]

# Définir les valeurs correspondant à chaque condition
choices = [
    'Value for FLIND, NRF, NNF',
    'Value for FLOOR',
    'Value for TUNNEL',
    'Value for TOT'
]

# Appliquer np.select pour obtenir les résultats
self._maquette_mapped_columns['Cap_Floor_Tunnel'] = np.select(conditions, choices, default='Default Value')

# Afficher le DataFrame résultant
print(self._maquette_mapped_columns)