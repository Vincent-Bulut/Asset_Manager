
<style>
  .list-container {
    height: 200px; /* Adjust as needed */
    overflow-y: auto;
  }
  .list-header {
    position: sticky;
    top: 0;
    background: white;
    z-index: 10;
  }
  button {
    /* Style your buttons */
  }
</style>

<div class="list-header">
  Recherche Client...
  <input type="text" bind:value={searchQuery} placeholder="Search clients...">
</div>
<div class="list-container">
  {#each filteredClients() as client}
    <div>{client}</div>
    <button on:click={() => addClient(client)}>Add</button>
  {/each}
</div>