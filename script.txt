

UPDATE TableA AS a
INNER JOIN TableB AS b ON a.Key = b.Key
SET a.Field1 = b.Field1, 
    a.Field2 = b.Field2;

INSERT INTO TableA (Key, Field1, Field2)
SELECT b.Key, b.Field1, b.Field2
FROM TableB AS b
WHERE NOT EXISTS (
    SELECT 1
    FROM TableA AS a
    WHERE a.Key = b.Key
);