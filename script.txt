<script>
  let users = ['Admin', 'Moderator', 'Visitor']; // Les options pour le dropdown
  let selectedUser = 'Admin'; // L'option sélectionnée par défaut

  // Fonction pour ajouter un utilisateur
  function addUser() {
    // Ici, vous pouvez ajouter la logique pour traiter l'ajout d'un nouvel utilisateur
    console.log("Ajouter un utilisateur");
  }
</script>

<style>
  .container {
    display: flex;
    flex-direction: column; /* Aligner les éléments verticalement */
  }
  .dropdown-container {
    display: flex;
    align-items: center;
    margin-bottom: 0.5em; /* Espacement entre le dropdown et le bouton */
  }
  select, button {
    margin: 0.5em;
  }
  .add-user-button {
    margin-left: 2em; /* Ajuster selon le style souhaité */
  }
</style>

<div class="container">
  <div class="dropdown-container">
    <select bind:value={selectedUser}>
      {#each users as user}
        <option value={user}>{user}</option>
      {/each}
    </select>
    
    <button on:click={addUser} class="add-user-button">+User</button>
  </div>
  
  <button>Update</button>
</div>
